class Sizes {
  const Sizes();
  final double screenPadding = 20.0;
  final int promoImageMinimalSize = 600;
  final int moduleImageMinimalSize = 600;
}

abstract class AppStyles {
  static const sizes = Sizes();
}
